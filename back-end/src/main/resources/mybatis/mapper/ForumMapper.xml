<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.nextLearnServer.mapper.ForumMapper">
    <insert id="publishQuestion" parameterType="com.example.nextLearnServer.pojo.Question">
        insert into question(title, content, role, publisher, commentList, category)
        values(#{title},#{content},#{role},#{publisher}, #{commentList}, #{category});
    </insert>

    <insert id="bookmarkQuestion" parameterType="com.example.nextLearnServer.pojo.Question">
        insert into question(title, content, role, publisher, commentList, category)
        values(#{title},#{content},#{role},#{publisher}, #{commentList}, #{category});
    </insert>
</mapper>