<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.nextLearnServer.mapper.CourseMapper">
    <select id="selectAllCourses" resultType="com.example.nextLearnServer.pojo.Course">
        select * from course;
    </select>

    <select id="selectCoursesByTeacherId" resultType="com.example.nextLearnServer.pojo.Course">
        select * from course where id = #{teacherId};
    </select>

    <select id="selectCoursesByStudentId" resultType="com.example.nextLearnServer.pojo.Course">
        select * from course where id = #{studentId};
    </select>

    <select id="selectCoursesByName" resultType="com.example.nextLearnServer.pojo.Course">
        select * from course where name like CONCAT('%', #{keyword}, '%');
    </select>
</mapper>